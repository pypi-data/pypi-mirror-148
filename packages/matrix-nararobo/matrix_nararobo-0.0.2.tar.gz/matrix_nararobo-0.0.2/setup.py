from setuptools import setup, find_packages

setup(
    name='matrix_nararobo',
    version='0.0.2',
    packages=find_packages(),
    author='aoi',
    description='A library that can calculate square matrices.'

)