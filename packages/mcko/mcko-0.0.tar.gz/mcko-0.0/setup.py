from setuptools import setup

setup(name='mcko',
      version='0.0',
      description='Gaussian and Binomial distributions',
      packages=['mcko'],
      author_email='gif1601@mail.ru',
      zip_safe=False)